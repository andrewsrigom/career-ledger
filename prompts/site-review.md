# Review the public site

Use `$career-maintain-site`.

Review the generated site for visual coherence, reading quality, responsive behavior, accessibility, broken internal links, GitHub Pages base-path behavior, and accidental exposure of private data.

Fix concrete issues, add tests where useful, and run `npm run check`.
